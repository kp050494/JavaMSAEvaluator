# Leave empty for local dev (Vite proxies /api and /ws to http://localhost:8080).
# For a split deploy (Vercel + Render), set this to your backend's public origin,
# e.g. https://spring-arena-backend.onrender.com
VITE_API_URL=
